#include "GeomPlaneMwpc.h"
#include <queue>
#include "math.h"

//ClassImp(GeomPlaneMwpc);

const float GeomPlaneMwpc::fF1_TICK = 128.e-9; // in ms

//  set<CCluster1>& GeomPlaneMwpc::Clusterize(const map<int,CDigit1>& digits) {

//    fClusters.clear();

//    for (map<int,CDigit1>::const_iterator ii = digits.begin(); ii!=digits.end(); ) {

    
//      int nchans = 1;
//      double res = Resolution(i->ch);
//      vector<double> data; 
//      data.push_back(time);
//      fClusters.insert(CCluster1(pos,nchans,res,data));
//    }
//    return fClusters;
//  }



