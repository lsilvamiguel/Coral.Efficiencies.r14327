      common/badlist/ ndead,
     +                idead(numc),
     +                noisy,
     +                inoisy(numc),
     +                rnoisy(numc)

      integer ndead,
     +        idead,
     +        noisy,
     +        inoisy

      real rnoisy
